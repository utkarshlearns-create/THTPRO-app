import 'package:flutter/material.dart';

/// Prep module dashboard — recent activity.
class PrepDashboardScreen extends StatelessWidget {
  const PrepDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('THT Prep')),
      body: const Center(
        child: Text('THT Prep Dashboard (Coming Soon)'),
      ),
    );
  }
}
