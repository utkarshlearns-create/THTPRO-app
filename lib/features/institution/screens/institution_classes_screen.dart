import 'package:flutter/material.dart';

/// Institution classes / batches management.
class InstitutionClassesScreen extends StatelessWidget {
  const InstitutionClassesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Classes / Batches (Coming Soon)'),
      ),
    );
  }
}
