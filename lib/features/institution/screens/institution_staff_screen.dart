import 'package:flutter/material.dart';

/// Institution staff management.
class InstitutionStaffScreen extends StatelessWidget {
  const InstitutionStaffScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Staff & Teachers (Coming Soon)'),
      ),
    );
  }
}
