import 'package:flutter/material.dart';

/// Institution student management.
class InstitutionStudentsScreen extends StatelessWidget {
  const InstitutionStudentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Enrolled Students (Coming Soon)'),
      ),
    );
  }
}
