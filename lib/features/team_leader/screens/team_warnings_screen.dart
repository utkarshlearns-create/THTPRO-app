import 'package:flutter/material.dart';

/// Warnings management screen.
class TeamWarningsScreen extends StatelessWidget {
  const TeamWarningsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Team Warnings (Coming Soon)'),
      ),
    );
  }
}
