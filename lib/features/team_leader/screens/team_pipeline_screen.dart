import 'package:flutter/material.dart';

/// Aggregated pipeline of the whole team.
class TeamPipelineScreen extends StatelessWidget {
  const TeamPipelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Team Pipeline (Coming Soon)'),
      ),
    );
  }
}
