import 'package:flutter/material.dart';

/// Team Leader's own pipeline (as a counsellor).
class MyPipelineScreen extends StatelessWidget {
  const MyPipelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Pipeline (Coming Soon)'),
      ),
    );
  }
}
