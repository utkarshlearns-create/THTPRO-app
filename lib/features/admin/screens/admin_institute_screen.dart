import 'package:flutter/material.dart';

/// Admin Institute screen — manages institutions.
class AdminInstituteScreen extends StatelessWidget {
  const AdminInstituteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Institutions (Coming Soon)'),
      ),
    );
  }
}
