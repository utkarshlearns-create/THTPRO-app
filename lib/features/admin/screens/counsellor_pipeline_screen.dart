import 'package:flutter/material.dart';

/// Counsellor Pipeline — manages clients and demos.
class CounsellorPipelineScreen extends StatelessWidget {
  const CounsellorPipelineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Counsellor Pipeline (Coming Soon)'),
      ),
    );
  }
}
